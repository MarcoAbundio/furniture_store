# Database (Neon PostgreSQL)
SPRING_DATASOURCE_URL=jdbc:postgresql://<host>:5432/<database>?user=<username>&password=<password>&sslmode=require&channelBinding=require

# JWT
JWT_SECRET=<your-secret-key-min-256-bits>
JWT_EXPIRATION=86400000
